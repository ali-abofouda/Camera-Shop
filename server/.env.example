# Server Configuration

# Port the backend runs on
PORT=5000

# Comma-separated origins allowed by CORS
CLIENT_ORIGINS=http://localhost:5173

# JWT secret for signing tokens (use a secure random string in production)
JWT_SECRET=change-me-to-a-secure-random-string

# Admin credentials
ADMIN_USERNAME=admin
# Default password: admin123 (bcrypt hash below)
# To generate a new hash: node -e "console.log(require('bcryptjs').hashSync('your-password', 10))"
ADMIN_PASSWORD_HASH=$2b$10$dXJ3se.kBwKsFcwTmyaKOeY.bakdUSHh3D1ynjUTduVdJN9WewtG6

# SQLite database file path (default: server/data.sqlite)
# SQLITE_PATH=./data.sqlite

# Upload directory for product images (default: server/uploads)
# UPLOAD_DIR=./uploads
